{
  "name": "virgil-registry-api",
  "version": "0.0.0",
  "devDependencies": {
    "@cloudflare/workers-types": "^3.16.0",
    "esbuild": "^0.15.7",
    "prettier": "^2.7.1",
    "typescript": "^4.8.2",
    "wrangler": "2.0.28"
  },
  "private": true,
  "scripts": {
    "build": "esbuild ./src --bundle --format=esm --minify --outdir=./dist",
    "start": "wrangler dev",
    "deploy": "wrangler publish"
  },
  "dependencies": {
    "itty-router": "^2.6.1"
  }
}
